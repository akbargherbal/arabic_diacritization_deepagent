# Skill: Meter Fitting (تقطيع عروضي)

## When to use
Loaded when the diacritizer subagent needs guidance beyond the basic system
prompt — e.g. meter-specific zihaf/illa conventions that are cheaper to keep
here (progressive disclosure) than to inline into every prompt.

## Contents (fill in per-meter as you build this out)

For each of the ~9-10 supported meters, this should eventually contain:
- Which zihafat are common/acceptable vs. rare/suspicious for this meter
- Classical examples of the meter correctly scanned, for few-shot grounding
- Common LLM failure patterns specific to this meter (e.g. confusing Taweel
  and Mutaqarib's foot counts)

## Status
Stub. Not filled in — this is exactly the kind of meter-specific domain
knowledge that shouldn't be guessed into a scaffold. Populate incrementally
as you observe real correction-pass failures per meter.
